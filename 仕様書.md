# 屋台レジ 仕様書

- 版数：1.1（商品編集・会計取消版 / sw キャッシュ regi-cache-v10）
- 構成：単一HTML（index.html）＋ Service Worker（sw.js）＋ manifest.json ＋ アイコン
- 配信：GitHub Pages（https://k2mac49.github.io/oomasa/）
- 実行環境：モバイルブラウザ（iOS Safari 想定）／ホーム画面PWA

## 1. システム構成
```
oomasa/ (= C:\DATA\Deveropment\oomasa\regi のローカル作業ツリー)
├─ index.html         … アプリ本体（UI＋ロジック、CSS/JS内包の単一ファイル）
├─ sw.js              … Service Worker（オフラインキャッシュ）
├─ manifest.json      … PWAマニフェスト
├─ icon-192.png       … アプリアイコン
├─ icon-512.png       … アプリアイコン（maskable）
├─ apple-touch-icon.png … iOSホーム画面アイコン(180px)
├─ 要件定義書.md
├─ 仕様書.md
└─ README.md
```
- 外部依存なし（CDN・フレームワーク不使用）。すべて内包。
- データはブラウザの localStorage に保存。サーバー側にデータは持たない。

## 2. 商品マスタ（⚙️設定タブで編集）
初期値（index.html 冒頭の `DEFAULTS` 配列）

| 商品名 | 単価 |
|---|---|
| 焼うどん | 600 |
| 豚バラ | 600 |
| 梅酒 | 500 |

初回起動時に `DEFAULTS` を localStorage（`regmock_prods_v1`）へ複製し、以後はそちらが正。
設定タブから 名称・単価の変更／追加／削除／並び替え／有効・無効 が行える。
各商品は書き換わらない `id` を持ち、個数と値引きはこの `id` をキーに保持する。
各商品は 有効／無効 を持ち、**有効のときだけ**レジ・値引きモーダルに出る
（無効でも設定タブには残るので、いつでも有効に戻せる）。

## 3. 画面構成
### 3.1 タブ（メニュー）
- 「🧾 レジ」「📊 売上」「⚙️ 設定」の3タブ。売上タブには会計件数バッジを表示。

### 3.2 レジ画面（横スワイプ2ページ＋スナップ）
- ページA「商品」
  - 上部固定ヘッダー：合計金額（スクロールしても固定）＋「値引き」ボタン。
  - 商品リスト：各行に 商品名／単価／小計／−／個数／＋。
    値引き中は単価に取消線＋実売価格（赤）を表示、行を警告枠。
    無効にした商品はこの一覧に表示しない。
    表示できる商品が0件のときは設定タブへの案内を表示。
- ページB「電卓」
  - 合計／預り金／おつり（不足時は警告色）。
  - クイック入金（+1000/+5000/+10000）、テンキー（0-9,00,←）。
  - 「取消」「会計する」。画面内に収まりスクロール不要。
- 下部に「🍢商品／🧮電卓」インジケータ（タップでも切替）。
- 会計確定後は自動で「商品」ページへ戻る。

### 3.3 売上画面
- 営業日（date）・イベント名（text）入力。会計時にこの値で記録。
- サマリ：総売上／営業日数／総会計数。
- 「CSVで書き出す／共有」「全消去」。
- 日付ごとのグループ（新しい順）：日別合計・会計数・販売点数・値引き計、
  品目別（実売・値引き額）、会計履歴。
- 各会計に「✕」ボタン。確認ダイアログののち、その1会計だけを削除できる。

### 3.4 値引きモーダル（商品ごと）
- 商品ごとに −100（値下げ）／＋100（定価まで戻す）／半額 を設定。
- 表示は「元値 −引いた額→ 実売価格」（例：¥600 −¥100→ ¥500）。
- 定価超過方向（＋）と単価100円以下方向（−）はボタンを無効化（グレーアウト）。
- 「この内容で適用」「すべて解除」「閉じる」。
- 商品マスタの変更は、モーダルを開くたびに反映される。

### 3.5 設定画面（商品と金額）
- 商品ごとに2行構成。
  - 1行目：商品名（左寄せ・幅の約2/3）／ ¥単価（右寄せ・最大4桁）
  - 2行目：「● 有効 ／ ○ 無効」トグル ／「削除」
  - 左端に `[↑][↓]`（並び替え）。
- 名称・単価は入力確定時（change）に保存する。入力途中に画面を作り直さないため、
  iOSでキーボードが閉じない。名称が空・単価が不正の場合は元の値に戻して警告する。
- 単価は4桁（0〜9999）まで。数字以外は除去して解釈する。
- 単価を変更すると、その商品の値引きは自動で解除される。
- **有効／無効トグル**：無効にするとレジの商品一覧と値引きモーダルから消える。
  そのとき個数は0に戻し、値引き設定も解除する。設定タブには淡色で残るので、
  もう一度押せばレジに戻る。販売停止の手段はこの1つに統一している
  （「売切」表示は役割が重複するため廃止）。
  トグル操作では一覧を作り直さず、押したボタンと行だけ更新する。
- 「削除」は確認ダイアログを出す。売上記録のある商品は、記録が残る旨を明記する。
- 「＋ 商品を追加」。
- 入力欄の文字サイズは16px（iOSの自動ズームを防ぐため）。

## 4. データモデル
### 4.1 localStorage キー
- `regmock_txns_v2` … 会計履歴（配列）※形式は従来どおり
- `regmock_prods_v1` … 商品マスタ `[{id, name, price, on}]`
  （`on`＝有効。旧形式の `sold`/`off` は読み込み時に `on` へ読み替える）
- `regmock_disc_v3` … 値引き設定 `{ discounts:{商品id:…}, date:"YYYY-MM-DD" }`
  （v2 は配列の位置がキーだった。商品編集の導入に伴い id キーへ変更）

### 4.2 会計レコード（txn）
```
{
  id:        連番,
  date:      "YYYY-MM-DD",   // 営業日
  event:     "イベント名",
  time:      "HH:MM",
  items: [ { name, price(定価), count, unit(実売単価) } ],
  subtotal:  定価ベース合計,
  discount:  値引き合計(= subtotal - total),
  total:     実売合計,
  received:  預り金,
  change:    おつり
}
```

### 4.3 値引き設定（discounts[商品id]）
- キーなし（値引きなし）／`{type:"amount", value:N}`（単価-N円）／`{type:"half"}`（半額）

## 5. 主要ロジック
### 5.1 実売単価 eff(p)
- 値引きなし → 定価
- half → floor(定価 / 2)
- amount → max(0, 定価 − value)

### 5.2 金額計算
- 合計（実売）= Σ eff(i) × 個数
- おつり = 預り金 − 合計。負なら「不足」を警告表示、会計不可。
- 「会計する」有効条件：1点以上 かつ 預り金 ≥ 合計。

### 5.3 値引きの境界
- ＋100（値上げ）：value = max(0, value−100)。value=0 で無効。
- −100（値下げ）：value = min(定価−100, value+100)。実売100円で無効（0円以下不可）。
- 営業日（date）変更時、値引き設定を自動解除。

### 5.4 会計処理
- items を確定（name/price/count/unit）→ txn を作成し localStorage 追記。
  商品名・単価は会計時点の値を保存するため、あとで商品マスタを変えても記録は変わらない。
- 個数と預り金をリセット（値引き設定は継続）。売上表示を更新。
- 会計直後8秒間、トーストの「取消」で直前の会計を取り消せる。取消時、次の客をまだ
  打っていなければ（個数0かつ預り金0）カートを復元し、打ち直せるようにする。
- 売上タブの各会計「✕」でも個別に削除できる（確認ダイアログあり）。

### 5.5 保存失敗時の扱い
- localStorage への書き込みは成功/失敗を返す。失敗時は赤いトーストで警告し、
  成功トースト（会計完了・値引き適用など）で上書きしない。

### 5.6 集計（売上）
- 日付でグルーピング（降順）。品目別は実売金額を積算、
  値引き額 = Σ(定価−実売)×個数。日別合計 = Σ txn.total。

## 6. CSV仕様
- 文字コード：UTF-8（BOM付き）。改行 CRLF。
- 1行＝1明細（会計×商品）。会計単位の値は各会計の先頭行にのみ記載。
- 列：
  `日付, イベント, 会計No, 時刻, 商品, 通常単価, 適用単価, 単価値引き,
   個数, 金額, 会計小計, 会計値引き計, 会計合計, 預り金, おつり`
- 書き出し：navigator.share（対応時＝iOSで共有シート）→ 非対応時はダウンロード。
- ファイル名：`売上_YYYY-MM-DD.csv`

## 7. PWA / オフライン仕様
- manifest.json：display=standalone、orientation=portrait、テーマ色 #0f172a、アイコン定義。
- sw.js：
  - `CACHE = "regi-cache-v10"`（キャッシュを丸ごと捨てたいときに版数を上げる）。
  - install 時にアプリ一式をプリキャッシュ。
  - 画面（HTML）は**ネット優先**。2.5秒で応答がなければキャッシュへ切り替えるため、
    電波が弱い現場でも起動が待たされない。取得できた応答はキャッシュへ書き戻す。
  - アイコン・manifest などはキャッシュ優先＋ネット取得分を追記。
- 更新反映：push すれば、次にオンラインで開いた時点で最新のHTMLが届く。
  ホーム画面アイコンの削除・再追加やサイトデータ消去は不要。

## 8. デプロイ手順（GitHub Pages）
1. リポジトリ（Public）に index.html / sw.js / manifest.json / アイコンを配置。
2. Settings → Pages → Deploy from a branch → main / (root)。
3. push すると自動でビルド・再公開（反映まで数分）。
4. 更新時は該当ファイルを上書き commit → push。CACHE版数を上げること。

## 9. 既知の制約
- 単一端末・ローカル保存のみ（多端末同期なし）。恒久保存はCSVで担保。
- iOSはローカル保存を長期未使用時に消す場合がある。
- 商品名を変更すると、売上集計は商品名でグルーピングするため、旧名と新名が別項目
  として並ぶ（記録済みの売上を書き換えない仕様のため）。
- 完全オフラインで起動した場合は、直近に取得したHTMLで動作する。

## 10. 変更履歴（要約）
- v1：商品ボタン＋個数＋合計＋預り金／おつり。
- v2〜：売上タブ（会計別・品目別・売上合計）。
- v3〜：営業日・イベント名、日付別集計、localStorage永続化。
- v4〜：値引き（当初は小計ベース→単価一律→商品ごとへ）。
- v5〜：時間帯セール（継続・日付/手動解除）、電卓レイアウト固定。
- v6〜：商品ごと値引き、電卓側の値引き表示を撤去、売上側に内訳。
- v7：値引き±の直感化＋境界グレーアウト。
- v8：値引き表示を「元値→実売」形式に。
- 縦画面版：商品／電卓の横スワイプ＋スナップ、合計固定、余白圧縮、
  PWA化（sw.js/manifest/アイコン、CACHE=v9）。
- v1.1：
  - スワイプのスナップ強化（scroll-snap-stop）。CACHE=v10。
  - 会計の取消（直後8秒のトースト／売上履歴からの個別削除）。
  - HTMLをネット優先配信に変更し、更新が端末へ自動で届くようにした。
  - 保存失敗を赤トーストで通知（従来は無警告で消えていた）。
  - 商品マスタを設定タブから編集可能に（追加・削除・並び替え）。
    内部を配列の位置キーから商品idキーへ変更。
  - 設定画面：商品名（左・広め）と単価（右・4桁）を同じ行に配置。
  - 販売の可否を 有効／無効トグル に一本化（当初の「売切」表示は廃止）。
  - 「初期の3品に戻す」は誤操作の危険が利点を上回るため削除。
  - 商品名・イベント名のHTMLエスケープを追加。
