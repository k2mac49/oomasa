# 屋台レジ 要件定義書

- プロジェクト名：屋台レジ（oomasa / regi）
- 版数：1.1
- 対象アプリ：単一HTMLのPWA（GitHub Pages 配信）
- 公開URL：https://k2mac49.github.io/oomasa/

## 1. 背景・目的
屋台・イベント出店での会計を、専用レジ機やPOSアプリを導入せずに、手持ちの
iPhone 1台で完結させる。電波が不安定な現場でも確実に動作し、売上を後から
集計・書き出しできることを目的とする。

## 2. 対象ユーザー・利用シーン
- 利用者：出店者本人（1〜数名）。ITに詳しくない担当者でも操作できること。
- 端末：iPhone（縦画面を主に想定）。iPad でも利用可。
- 環境：電波が弱い／無い屋外を想定。オフラインで完全動作すること。
- 運用：1端末＝1レジ。1日ごと・イベントごとに売上を記録する。

## 3. スコープ（機能要件）
### 3.1 会計（レジ）
- 商品ボタンで販売点数を加減算（＋／−）し、合計を自動計算する。
- テンキーで預り金を入力し、おつりを自動表示する。預り金が不足のときは
  「不足 ¥xxx」を警告色で表示し、会計を確定できない。
- 「会計する」で1会計を確定・記録する。
- 「取消」は電卓の C／AC と同じ感覚で使えること。1回目は預り金だけを消し、
  もう一度押すと商品の個数も消す。次に何が消えるかがボタンに表示されること。
- 打ち間違いに備え、確定した会計を取り消せること。直後はワンタップで、
  それ以降は売上履歴から1会計ずつ削除できること。
- 画面は「商品」と「電卓」を左右スワイプで切り替え、どちらかにスナップする。

### 3.2 値引き（時間帯セール・商品単位）
- 商品ごとに単価から値引きを設定できる（定額引き＝100円単位、または半額）。
- 値引きは適用したら解除するまで継続する（会計をまたいでも有効）。
- 解除条件：手動解除、または営業日を変更したときの自動解除。
- 単価は定価を超えて上げられない／0円以下にはできない（下限100円）。
- 値引き適用中は、対象商品の価格に取消線＋実売価格を表示し、
  画面全体を警告表示（値引きボタンのハザード表示など）で明示する。

### 3.3 売上集計
- 営業日・イベント名を設定でき、会計はその日付・イベントで記録する。
- 売上タブで、日付ごとに以下を表示する。
  - 日別の売上合計・会計数・販売点数
  - 品目別の販売個数・実売金額（値引きがあれば値引き額も表示）
  - 会計履歴（1会計ごとの明細、預り／おつり、値引きがあった会計は内訳）
- 値引きが0の会計は値引き表示を出さず通常記載。値引きがある場合のみ額を表示。
- 総売上・営業日数・総会計数のサマリを表示する。

### 3.4 商品マスタの編集
- 商品名・単価を画面から変更できる。商品の追加・削除もできる。
- よく出る品を上に置けるよう、並び順を変更できる。
- 商品ごとに 有効／無効 を切り替えられ、有効のときだけレジに表示・機能する。
  今日出さない商品・完売した商品は無効にしておけばよい（削除ではないので元に戻せる）。
- 変更は次の会計から反映され、記録済みの売上には影響しないこと。

### 3.5 データ書き出し
- 売上をCSVで書き出し・共有できる（iPhoneのAirDrop／メール／LINE／ファイル保存）。
- CSVはExcelでの集計を前提とし、UTF-8（BOM付き）で文字化けを防ぐ。
- 商品ごとの値引き額を列として持ち、後からピボット集計できること。

## 4. 非機能要件
- オフライン：初回オンライン表示後は、電波が無くても全機能が動作すること。
- 保存：会計データは端末内に自動保存され、リロード・再起動後も保持されること。
  保存に失敗した場合は、気づけるように警告を表示すること（黙って消えないこと）。
- 更新：アプリを更新したら、端末側で特別な操作をしなくても次回起動時に反映されること。
- 可用性：無料の範囲で運用できること（GitHub Pages / Free）。
- 操作性：屋台のカウンターで片手・タッチ操作しやすいボタンサイズ。
- 表示：縦画面で「何を買ったか」が潰れないレイアウト。文字周りの余白は詰める。
- 表示：ブラウザのアドレスバーがある状態でも、電卓の「取消／会計する」が
  画面外に出ないこと（iPhone以外の端末・ブラウザを含む）。
- 移植性：単一HTML＋付随ファイルのみ。特別なサーバー・DBを必要としない。

## 5. 制約・前提
- 1端末運用。データは端末ローカル（localStorage）に保存され、他端末と同期しない。
- iOSの仕様上、長期間未使用だとブラウザがローカル保存を消す可能性がある。
  → 恒久保存はCSV書き出しによるバックアップで担保する。
- 配信は GitHub Pages（Publicリポジトリ、HTTPS）。Service Worker でオフライン化。
- 商品マスタの初期値は3品（焼うどん600円／豚バラ600円／梅酒500円）。
  以後は端末ごとに設定タブで編集でき、内容は端末内に保存される。

## 6. 対象外（今回のスコープ外）
- キャッシュレス決済・レシート印刷・在庫管理。
- 複数端末でのリアルタイム同期、ユーザー認証、クラウド保存。

## 7. 今後の拡張候補
- 締めの現金合わせ（釣銭準備金と実際の現金から差異を出す）。
- JSON形式のバックアップ／復元（CSVは書き出し専用で復元できないため）。
- 営業中の画面消灯防止（Wake Lock）。
- 個数の直接入力（＋の連打を減らす）。
- 日次サマリの自動バックアップ（共有先固定）。
- 複数端末運用時のデータ集約（クラウド or 手動CSV統合）。
