# CLAUDE.md — 屋台レジ（oomasa）

Claude Code 向けの引き継ぎメモ。作業前にこのファイルと `仕様書.md` を読むこと。

## プロジェクト概要
- 屋台・イベント向けのレジ。iPhone 1台（またはiPad）で完結する単一HTMLのPWA。
- 公開：GitHub Pages（https://k2mac49.github.io/oomasa/）。main ブランチの root を配信。
  **push＝公開作業そのもの**。main に入った時点で自動デプロイが走る（約45秒）。

## 作業場所が2つある（最重要）
- Windows：`C:\DATA\Development\oomasa\repo`
- Claude Code on the web（クラウドのコンテナ。セッションごとに clone し直す）

**どちらで作業する前にも `git pull origin main` を実行すること。**
2026-09-22 のセッションでは、Windows 側の v1.14〜v1.16 が push されておらず、
クラウド側が v1.13 を見ている状態から始まった。作業を始める前に版数を突き合わせる：

```
git log --oneline -1
grep -o 'APP_VERSION = "[0-9.]*"' index.html
```

旧 `C:\DATA\Development\oomasa\regi` は Git 管理外のただのフォルダなので使わない
（`仕様書.md` 冒頭の「Deveropment\oomasa\regi」という記載は古い）。

## 構成
- `index.html` … UI・ロジック・CSS をすべて内包した単一ファイル
- `sw.js` … Service Worker（HTMLはネット優先、2.5秒でキャッシュに切替）
- `manifest.json` / アイコン類
- `仕様書.md`（版数・変更履歴・データモデル）、`要件定義書.md`、`README.md`、`CLAUDE.md`（このファイル）

## 守るべき方針
- **外部依存なし**（CDN・フレームワーク不使用）。オフラインで動くことが前提。グラフも canvas で自前描画。
- データは端末の localStorage のみ。サーバーは持たない。
- 取り消せない操作（削除・全消去など）は必ず confirm を挟む。
- コメントは日本語で「なぜそうしたか」を書く（既存コードの書き方に合わせる）。
- 数値表記は全桁カンマ区切り（`¥150,000`）。「万」などの漢数字表記は使わない。
  割合は常に小数1桁（`14.0%`。丸めて末尾の0を落とすと桁が揃わず読みにくい）。
  ただし個数は普段が整数なので小数を出さない（1日平均のときだけ小数になる）。
- 変更を出すときは次の4点をセットで更新する：
  1. `index.html` の `APP_VERSION`（設定タブ最下部に表示）
  2. `sw.js` の `CACHE`（`regi-cache-vNN`。キャッシュを確実に入れ替えるため毎回上げる）
  3. `仕様書.md` の版数行と「10. 変更履歴」
  4. `README.md`（機能一覧・現行キャッシュ名）

## 現在の状態（v1.18 / regi-cache-v28）

### レジ画面
- **表示形式**：設定 → 🖥️ レジの表示 で表形式／カード式（`regmock_view_v1`）。
- **レイアウトの高さのしきい値は 601px に統一**（v1.18）。ここは触るとき必ず揃えること：
  - 「商品と電卓を左右に並べる」＝ `min-width:760px` かつ `min-height:601px`
  - 「カード式」＝ `orientation:landscape` かつ `min-width:900px` かつ `min-height:601px`
  - `601px` は既存の「横長かつ `max-height:600px`＝スマホ横（電卓を左右2分割）」のちょうど裏返し。
  - **560px まで下げてはいけない**。電卓2分割と重なって3列になり、クイック入金の「+1000」が切れる。
  - v1.17 以前は カード式560px／左右2列700px と割れており、その間の高さ
    （iPad mini 8.3 を Safari で開いたとき＝約624px）で、カードは効くのに左右2列にならず、
    商品が全幅6列・電卓はスワイプ、という想定していない見え方になっていた。

### 売上タブ
- **売上の分析**：
  - 範囲：日別／イベント全体（同じイベント名の全日程を時刻で重ねる。2日以上なら合計／1日平均）
  - 日別では、日付の選択肢と見出しに**その日のイベント名**を出す（v1.17）。
    同じ日に複数あるときは「イベント2件」と出し、絞り込みは隣の選択で行う。
  - 表示：商品別合計（横棒・多い順）／積み上げ（商品別）／時間帯合計（単色棒）／ヒートマップ
  - 集計単位 10分／30分／1時間、金額／個数
  - 累計線は常に全商品の合算。右目盛りはその範囲の最終累計から決めて固定
  - ヒートマップ：横＝時間帯、縦＝商品、色＝金額（青→黄→赤、RdYlBu反転）。色の基準は全体／商品ごと
  - 見出しの組み立ては `anlHead()` に1本化してある（グラフ側と商品別合計側で重複していた）
  - 表示設定は `regmock_anl_v1` に保存
- **日ごとの詳細の折りたたみ**（v1.17）：「品目別（実売）」「会計履歴」は `<details>` で開閉。
  見出しに品目数・会計件数を出すので畳んだままでも規模が分かる。
  既定は 品目別＝開く／会計履歴＝閉じる。開閉は `regmock_fold_v1` に記憶する。
  - **記憶は必須**。会計を1件消すと一覧を丸ごと作り直すため、覚えないと開いていた場所が閉じる。
  - **`innerHTML` で `open` 付きの `<details>` を生成すると、その直後にも `toggle` が飛ぶ。**
    素直に書くと描画のたびに既定値まで保存へ書き込まれるので、既定と違うものだけを記録する。
  - **`toggle` は bubbling しない**ので capture で拾っている。

### テストデータ
- 設定 → 🧪 テストデータ。1日分（2026-01-01 8:00〜15:00、241会計）／
  3日分（+1/2, 1/3、計539会計）。乱数の種を固定しているので毎回同じ内容。
  会計に `test:true` を付け、一括削除可。未書き出し件数からは除外。
- 本番にも残す方針（2026-09-22 に判断）。一括削除できるため。

## localStorage キー（表示の変更では絶対に触らない）
`regmock_txns_v2`（会計履歴）／`regmock_prods_v2`（商品マスタ）／`regmock_disc_v3`（値引き）／
`regmock_events_v1`（営業日ごとのイベント）／`regmock_view_v1`／`regmock_anl_v1`／
`regmock_fold_v1`（売上詳細の開閉）／`regmock_backup_v1`／`regmock_migrations`

**見せ方だけを変える改修では、これらに触れない。** 触らない限り履歴はそのまま残る。
変更前後で会計件数が変わらないことを実際に確かめてから出すこと。

## 未解決・要検証
- [ ] `manifest.json` の `orientation` が `"portrait"`。iPadのホーム画面アプリでは無視されるはずだが、
      Androidタブレットでは縦固定になる可能性あり（`"any"` への変更を検討）
- [ ] 日付をまたぐ営業（例 18:00〜翌1:00）は非対応。会計の時刻が "HH:MM" のみで日付の区切りを持たないため、
      0時台が朝扱いになり横軸が間延びする
- [ ] 1日平均は「その時間帯に営業していた日数」で割るため、累計線の終点が上部の「1日平均（合計÷日数）」より大きくなる。
      紛らわしければ単純な日数割りに変更
- [ ] 10分単位のヒートマップはマスが細く、全桁の数値が入らないため数値を省略している（列タップの内訳で確認）
- [ ] 「時間帯合計（単色棒）」は依頼の読み違いから生まれた表示。不要なら削除候補
- [ ] ヒートマップを本採用するかは、実データで使ってから判断（ユーザーの意向）
- [ ] 売上詳細の既定（会計履歴＝閉じる）が現場に合うかは、実際に使ってから判断

### 解消済み（2026-09-22）
- ~~iPad横でカード式の10品目めはスクロールが必要~~
  … iPad mini 8.3／10.9／Air・Pro 11／Pro 12.9 の横置きを実測し、いずれも再現しないことを確認。

## 動作確認の方法
外部依存がないので、ブラウザに読ませるだけで確かめられる。出す前に最低この2つは通すこと。

**1. 構文チェック** — `<script>` の中身を `new Function()` に通す（node で可）。
`sw.js`（同じく `new Function()`）と `manifest.json`（`JSON.parse`）も一緒に見る。

**2. 描画・動作確認** — Playwright のヘッドレス Chromium。
ローカルに静的サーバーを立てて（`npx http-server -p 8765 -c-1`）読ませる。
`file://` だと Service Worker の登録が失敗して余計なエラーが出るため。

- 画面サイズは最低 iPad横（1180×820）と iPhone縦（390×844）の2つ。
  レイアウトのしきい値を触ったときは、**しきい値の前後1pxと、実機のサイズ**を測る。
  iPad は mini 8.3（1133×744）／10.9（1180×820）／Air・Pro 11（1194×834）／
  Pro 12.9（1366×1024）。Safari で開く場合は上下のバーぶん約120px引く。
- `console` の error と `pageerror` を拾って0件であることを確認する。
- テストデータを投入すれば、実データなしで売上・分析の画面まで確認できる。
- 数えるより測る。「何品目が画面内に収まっているか」「ボタンがはみ出していないか」は
  `getBoundingClientRect()` で数値にして比べると、目視の印象より早く確実に分かる。

## 進め方
```
① ブランチを切る      作業用の枝。main は触らない
② 変更してコミット    なぜ変えたかを書く
③ push               枝をGitHubへ上げる（まだ公開されない）
④ PRを作る            差分と理由をまとめる（一人開発なら省略可）
⑤ マージ              main に入る → Pages が自動で公開（約45秒）
⑥ pull               もう一方の作業場所を追従させる
```
- 戻すのは `git revert` で足りる。データには触れていないので履歴は無傷。
- ただし実機に届くまでは人の操作（アプリを完全終了 → 電波のある場所で開き直す）が要る。
  **営業当日の朝にデプロイしない。** 前日までに出して実機で確認する。
